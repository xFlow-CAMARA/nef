httpVersion: 2
fqdn: core-simulator
sbiPort: 8080
oamPort: 8081
initOnStartup: true #will require a default config 
simulationProfile:
  plmn:
      mcc: "001"
      mnc: "01"
  dnn: "intenet"
  slice:
    sst: 1
    sd: "FFFFFF"
  numOfUe: 5
  numOfgNB: 3
  arrivalRate: 2

# you can define here a simultation profile that will be automatically configured
# useful when not running in standalone mode / for CICD
